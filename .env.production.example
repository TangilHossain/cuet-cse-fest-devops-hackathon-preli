# Production Environment Configuration
# IMPORTANT: Use strong, unique values in production!

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=admin_prod
MONGO_INITDB_ROOT_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
MONGO_URI=mongodb://admin_prod:YOUR_PASSWORD@mongo:27017
MONGO_DATABASE=hackathon_prod
MONGO_DB_NAME=hackathon_prod

# Application Ports (DO NOT CHANGE)
BACKEND_PORT=3847
GATEWAY_PORT=5921

# Environment
NODE_ENV=production

# Security Recommendations:
# 1. Use strong passwords (min 16 characters, mix of letters, numbers, symbols)
# 2. Never commit .env files to git
# 3. Use different credentials for dev and prod
# 4. Rotate credentials regularly
# 5. Store production secrets in secure vault (e.g., AWS Secrets Manager, HashiCorp Vault)
